services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: nona
      POSTGRES_PASSWORD: nona
      POSTGRES_DB: nona
    ports:
      - "5432:5432"
    volumes:
      - nona-postgres:/var/lib/postgresql/data

volumes:
  nona-postgres:
